Robot App: by Raul Sobon

Program supports input via filename arguments or stdin console.

usage:

bin/robot [FILENAME]

cat FILENAME | bin/robot


Make sure chmod +x bin/*

data/    contains many test files
lib/     contains the class libs
bin/     contains the starter file
run.sh   runs one example test 

